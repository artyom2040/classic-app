# Environment Configuration for Context Composer
# Copy this file to .env and fill in your actual values

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================

# Your Supabase instance URL
# Development: http://localhost:54321
# Production: https://api.artyom2040.com
EXPO_PUBLIC_SUPABASE_URL=https://api.artyom2040.com

# Supabase Anonymous (Public) Key
# Get this from: https://studio.artyom2040.com → Settings → API
# This key is safe to use in your mobile app
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key-here

# Data Source Configuration
# Options: local | supabase | firebase | api
# - local: Use JSON files from src/data/ (no backend required)
# - supabase: Fetch from Supabase (requires URL and keys above)
# - firebase: Use Firebase Firestore (not yet implemented)
# - api: Use custom REST API (not yet implemented)
EXPO_PUBLIC_DATA_SOURCE=local

# =============================================================================
# AUTHENTICATION
# =============================================================================

# Auth redirect URL for OAuth/Magic Link callbacks
# Format: <app-scheme>://auth/callback
# For production, register this in Supabase dashboard
EXPO_PUBLIC_AUTH_REDIRECT_URL=classicapp://auth/callback

# =============================================================================
# OPTIONAL: DEVELOPMENT OVERRIDES
# =============================================================================

# Uncomment these for local Supabase development
# EXPO_PUBLIC_SUPABASE_URL=http://localhost:54321
# EXPO_PUBLIC_DATA_SOURCE=local

# =============================================================================
# IMPORTANT SECURITY NOTES
# =============================================================================
#
# 1. NEVER commit your actual .env file to git
# 2. NEVER use SERVICE_ROLE_KEY in your mobile app
#    (Service role key should only be used in server-side scripts)
# 3. The ANON_KEY is safe to expose in your app
#    (it only allows RLS-protected operations)
# 4. For production builds, use Expo Secrets:
#    npx eas secret:create --name EXPO_PUBLIC_SUPABASE_URL --value <url>
#    npx eas secret:create --name EXPO_PUBLIC_SUPABASE_ANON_KEY --value <key>
#
# =============================================================================
